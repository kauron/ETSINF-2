#include <stdio.h>

#define PI 3.1416
main() {   
   float area, radius;
   radius = 10;   
   area = PI * (radius * radius);   
   printf("The area of the circle with radius %f is %f\n", radius, area); 
}

